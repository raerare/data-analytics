
# I used simple variables, math formulas, loops, and if statements
# to solve the problem step by step.

customer_id = 1001  # Assumed numeric ID; could be a string if IDs include letters or leading zeros.
customer_name = 'Jamie Rivera'  # Assumed full name in one field; might need first and last name separately.
customer_gender = 'Nonbinary'  # Assumed text value; could use standardized codes depending on business rules.
customer_date_of_birth = '1995-04-12'  # Stored as a string for now; real systems may use a date type.
drivers_license_number = 'D1234567'  # Stored as string because license numbers can include letters.
auto_policy_number = 'AP-987654'  # Stored as string because policy numbers may include letters or dashes.

my_name = 'Student Name'
hometown = 'Chicago, IL'

# Python reserved words:
# False, None, True, and, as, assert, async, await, break, class, continue, def, del,
# elif, else, except, finally, for, from, global, if, import, in, is, lambda, nonlocal,
# not, or, pass, raise, return, try, while, with, yield

# if: starts a conditional block that runs code only when a condition is true.
# else: provides an alternative block when an if condition is false.
# for: starts a loop that repeats for each item in a sequence.
# while: starts a loop that repeats while a condition remains true.
# def: defines a reusable function. A ^^function^^ is a named block of code.

